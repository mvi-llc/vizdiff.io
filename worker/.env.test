# Environment for `yarn test` (NODE_ENV=test). The database-integration tests (shard.test.ts)
# connect to the same local/CI Postgres the api tests use and create their own scratch database
# (vizdiff_worker_test) so they never touch the api's synchronized test schema.
POSTGRES_DATABASE=vizdiff_test
